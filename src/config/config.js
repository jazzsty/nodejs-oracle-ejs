const config = {
    cookieSecret: 'T0P-S3cR3t_cook!e',
    cookieName: 'ANOMS-Cookie.sid',
    listeningPort: 8082,
    isProd: false,
    // isProd: process.env.NODE_ENV === 'prod',
    // db: db,
};

export default config;
